package com.example.cs5520finalproject;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

import androidx.annotation.Nullable;

public class RankingsUserCard extends View {
    public RankingsUserCard(Context context) {
        super(context);
    }

    public RankingsUserCard(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public RankingsUserCard(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public RankingsUserCard(Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
